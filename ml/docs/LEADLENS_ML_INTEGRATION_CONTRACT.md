# Integration Contract

The repository must provide an immutable account-level snapshot with signal, evidence, company and ICP-fit fields plus stable company, monitor, run and timestamp keys. The ML package returns a versioned PredictionResult and separate ShadowRankingResult. Missing model or incompatible schema must fall back to the deterministic baseline without failing report delivery.
