import assert from"node:assert/strict";import * as r from"../../lib/discovery/account-first-validation-results";
assert.equal(r.ACCOUNT_FIRST_VALIDATION_RUN.run_id,"amor_account_first_validation_v1");assert.equal(r.ACCOUNT_FIRST_VALIDATION_RUN.calls,4);assert.equal(r.ACCOUNT_FIRST_VALIDATION_RUN.errors,0);assert.equal(r.ACCOUNT_FIRST_VALIDATION_RUN.historical_calls_after,46);assert.equal(r.ACCOUNT_FIRST_ECOSYSTEM_RESULTS.length,4);assert.ok(r.ACCOUNT_FIRST_VALIDATION_FUNNEL.entities_harvested<=20);assert.ok(r.ACCOUNT_FIRST_VALIDATION_FUNNEL.official_domains_resolved<=10);assert.ok(r.ACCOUNT_FIRST_VALIDATION_FUNNEL.business_models_verified<=6);assert.ok(r.ACCOUNT_FIRST_VALIDATION_FUNNEL.qualified_challengers<=4);assert.equal(r.ACCOUNT_FIRST_VALIDATION_FUNNEL.wasted_enrichments,0);assert.ok(r.ACCOUNT_FIRST_ENTITY_RESULTS.every(x=>!x.deep_enriched&&!x.qualified&&x.gate_rules.length));assert.equal(r.ACCOUNT_FIRST_ROUTE_VERDICTS.length,4);assert.equal(r.ACCOUNT_FIRST_ARCHITECTURE_VERDICT.level,"INCONCLUSIVE");assert.equal(r.ACCOUNT_FIRST_NEXT_DECISION.state,"RETURN TO SOURCE-MAP DESIGN");assert.equal(r.ACCOUNT_FIRST_PORTFOLIO_GUARD.v3r3_preserved,true);assert.equal(r.ACCOUNT_FIRST_PORTFOLIO_GUARD.v3r4_created,false);assert.equal(r.ACCOUNT_FIRST_VALIDATION_RUN.raw_payloads_persisted,false);
